apiVersion: v1
kind: Service
metadata:
  name: {{ include "authz.fullname" . }}-clip
  labels: 
    {{- include "authz.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    name: http